apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carpark-common.fullname" . }}-test-connection"
  labels:
    {{- include "carpark-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carpark-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
