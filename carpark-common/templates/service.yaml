apiVersion: v1
kind: Service
metadata:
  name: {{ include "carpark-common.fullname" . }}
  labels:
    {{- include "carpark-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "carpark-common.selectorLabels" . | nindent 4 }}
